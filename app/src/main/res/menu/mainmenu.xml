<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android" >
  <!--
    <item android:id="@+id/save_menu"
	  android:icon="@android:drawable/ic_menu_save"
	  android:title="@string/menu_save"></item>
	  -->
  <item android:id="@+id/undo_menu"
	android:icon="@android:drawable/ic_menu_revert"
	  android:title="@string/menu_undo"></item>
  <item android:id="@+id/reload_menu"
	  android:icon="@drawable/ic_action_refresh"
	  android:title="@string/menu_reload"></item>
    <item android:id="@+id/preference_menu"
	  android:icon="@android:drawable/ic_menu_preferences"
	  android:title="@string/menu_preference"></item>
</menu>
